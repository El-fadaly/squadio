import 'package:flutter/material.dart';

class CustomErrorIcon extends StatelessWidget {
  const CustomErrorIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.error);
  }
}
